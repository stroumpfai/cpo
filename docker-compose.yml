services:
  app:
    build: .
    ports:
      - "8002:8002"
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    env_file:
      - .env
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
